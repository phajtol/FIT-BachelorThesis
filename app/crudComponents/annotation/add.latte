{ifset $addAllowed}
	<button type="button" class="btn {ifset $button}btn-{$button}{else}btn-xs{/ifset} btn-success" data-toggle="modal" data-target="#AddNewModal{$uniqid}" title="Add new annotation">
		<span class="glyphicon glyphicon-plus"></span>
		{if isset($description) && $description == true} Add new annotation{/if}
	</button>
{/ifset}
