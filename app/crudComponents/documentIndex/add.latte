{ifset $addAllowed}
	<button type="button" class="btn {ifset $button}btn-{$button}{/ifset} btn-success" data-toggle="modal" data-target="#AddNewModal{$uniqid}" title="Add new document index">
		<span class="glyphicon glyphicon-plus"></span>
		{if isset($description) && $description == true} Add new document index{/if}
	</button>
{/ifset}
